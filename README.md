# DBテーブル設計・管理ガイドライン

本プロジェクトでは、AI（LLM）とGitでの変更履歴管理を両立するため、YAML 形式でテーブル設計（マスターデータ）を管理するよ
僕は使わないからみんなこれ落としていい感じに頑張って運用してね。
コードも1行も読んでないから内容は知らないよ。

---

## 1. 運用方針（アーキテクチャ）

 [人間 / AI] ──(修正・更新)──>  YAMLファイル (Git管理 / マスターデータ)
                                    │
                                    ├───① AIプロンプトへの読み込み用
                                    ├───② スクリプト実行 ──> Excel設計書 (閲覧・調査用)
                                    └───③ スクリプト実行 ──> SQL(DDL) / 型定義 (自動生成)
